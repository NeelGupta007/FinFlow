## Packages
recharts | For visualizing expense data (pie charts, trend lines)
framer-motion | For smooth page transitions and UI animations
date-fns | For formatting dates and time relative to now
clsx | For conditional class merging
tailwind-merge | For handling Tailwind class conflicts

## Notes
- Authenticated via Replit Auth (use-auth hook)
- Mobile-first design: using bottom navigation on small screens
- Expenses are stored in cents, need formatting util
- AI Parsing endpoint expects { text: string } and returns a partial expense object
